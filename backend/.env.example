PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=wmsm
DB_USER=postgres
DB_PASSWORD=你的密碼

# 開發環境：前端 Vite dev server 在 5173，需列入 CORS 白名單
# 正式環境：前後端同源（Node 同時服務靜態檔），此欄可留空或設為佈署網址
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

NODE_ENV=development
