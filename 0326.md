# 0326 工作日誌

## 2026-03-25

### 完成事項
- 依 Claude Code Project Structure 建立完整工程師駐守環境
- 將 `WMSMteb.html` 原型重構為 React TSX + Express TypeScript + PostgreSQL
- 修正 PostgreSQL schema 三個關鍵問題：
  1. Sequence forward reference → 改用 BEFORE INSERT Trigger
  2. `pool.connect()` 在 try 外 → 移入 try，使用 `client?.release()`
  3. Vite proxy port 3001 → 修正為 3000

### 系統現狀
- 後端：`http://localhost:3000`（Express + TypeScript）
- 前端：`http://localhost:5173`（React + Vite）
- 資料庫：PostgreSQL `wmsm`（9 張資料表 + 2 個 view）

### 待確認
- [ ] WMSM020 查詢採購單功能是否正常帶入品項
- [x] WMSM030 Excel 匯入上傳流程（已確認：`import_batches` 寫入正常，IMP-20260401-001）
- [ ] 列印紀錄歷史頁面統計數字
- [ ] UAT 簽核儲存至 DB

## 2026-04-01

### 完成事項
- 新增 `import_batch_items.ref_code` 欄位（VARCHAR(20) DEFAULT ''）
- WMSM030 匯入 Excel 範本含 10 欄（含對照號），範本下載 `/api/import/template`
- 確認 123.xlsx 上傳後正確寫入 `import_batches`（total_rows=0 為正常：範本僅含標題列）
- 修正 `ImportBatchItem` 含 `ref_code`，前後端型別一致

### 說明
123.xlsx / 範本 Excel 上傳流程：
1. `POST /api/import/preview` → 解析 Excel，寫入 `import_batches`（status='preview'）與 `import_batch_items`
2. `POST /api/import/execute` → 讀取 preview 批次，寫入 `print_jobs` + `print_job_items`，更新 status='executed'
3. 若 Excel 只有標題列（無資料），`total_rows=0`，`import_batch_items` 無資料列為預期行為

---

## 2026-04-08

### 完成事項

#### WMSM030 列印視窗
- 「轉檔結轉列印」確認後，自動開啟新視窗列印每箱 8cm×11cm 標籤（含尾數箱）
- 新建 `frontend/src/utils/printLabels.ts`（共用 `LabelData` interface + `printLabels()` 函式）
- WMSM020 改 import 共用版，移除重複程式碼
- WMSM030 新增 `labelDataList030()` 展開每箱標籤資料

#### Code Review 修正（全部完成）
- 🔴 `executeImport`：BEGIN 後兩個早期 return 前加 `await client.query('ROLLBACK')`
- 🟡 `executeImport`：新增 `batch_no` / `operator` 空值驗證，缺值回 400
- 🟡 `printLabels.ts`：新增 `esc()` HTML escape，所有使用者可控欄位套用
- 🔵 `executeImport`：SELECT + INSERT `print_job_items` 補 `ref_code`
- 🔵 密碼安全：安裝 `bcrypt`，`auth.ts` 改用 `bcrypt.compare()`，schema 種子密碼改為 hash（cost=10）
- Bug fix：WMSM020 `shelfDays` 初始值 `'365'`（字串）→ `365`（數字）

#### 忘記密碼功能
- 後端：`POST /api/auth/reset-password`（驗證帳號存在 → bcrypt hash → 更新 DB → 回傳 hash）
- 前端：`ForgotPassword/index.tsx` — 輸入帳號 + 新密碼 → 更新成功後顯示：
  - 🔑 新密碼明文（可複製，用於返回登入頁貼上）
  - 🔒 bcrypt 雜湊值（可複製，供管理員確認）
  - 「← 返回登入並自動填入密碼」按鈕（自動帶入密碼欄）
- 登入頁下方加「忘記密碼？」連結

### 測試帳號（密碼：wmsm2026）
| 帳號 | 角色 |
|------|------|
| admin | 系統管理員 |
| warehouse01 | 倉儲人員甲 |
| warehouse02 | 倉儲人員乙 |
| qa01 | 品管人員 |
